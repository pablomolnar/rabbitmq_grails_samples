import org.springframework.amqp.core.*
import org.springframework.amqp.core.Binding.DestinationType

// Place your Spring DSL code here
beans = {
/*    myQueueName(Queue, 'myQueueName')

    direct(DirectExchange, 'my.exchange.direct')
    directBinding(Binding, 'myQueueName', DestinationType.QUEUE, 'my.exchange.direct', 'myQueueName', null)

    headers(Binding, 'myQueueName', DestinationType.QUEUE, 'amq.headers', '', [prime: true, 'x-match': 'all'])
    */
}